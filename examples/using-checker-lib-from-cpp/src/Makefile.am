bin_PROGRAMS = using-checker

using_checker_SOURCES = using-checker.cpp
using_checker_CXXFLAGS =
using_checker_LDADD = -ldivvun -lcg3 -lhfst -larchive
# TODO: use pkg-config here?
# TODO: do I really need to -l into all the stuff that divvun -l's into?
